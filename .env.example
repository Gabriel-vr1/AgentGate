# Optional Foundry configuration. No secrets are needed: uses authenticated Azure CLI.
# Copy to .env locally; never commit .env.
AGENTGATE_ENV=development
AZURE_AI_PROJECT_ENDPOINT=https://agentgate-foundry-gvr0710.services.ai.azure.com/api/projects/agentgate
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-4.1-mini
# Traces are saved locally via --trace, not exported to Application Insights.
